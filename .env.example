# Database
POSTGRES_DB=mindstore
POSTGRES_USER=mindstore
POSTGRES_PASSWORD=change_me_strong_password

# API key for ingestion and MCP server authentication
API_KEY=change_me_strong_api_key

# Web UI (local network only, port 3000)
WEBAPP_PORT=3000

# Ingestion service
INGESTION_HOST=0.0.0.0
INGESTION_PORT=8000

# MCP server
MCP_HOST=0.0.0.0
MCP_PORT=8001

# Embedding model (swap to 'voyage' later without restructuring)
EMBEDDER_BACKEND=local
EMBEDDER_MODEL=all-MiniLM-L6-v2

# Optional: Voyage AI (for future integration)
# VOYAGE_API_KEY=
